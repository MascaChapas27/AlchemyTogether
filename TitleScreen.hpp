#ifndef __TITLE_SCREEN_HPP__
#define __TITLE_SCREEN_HPP__

class TitleScreen {
    public:
        // Returns a joystick code if a joystick button was pressed, -1 if not
        int run();
};

#endif // __TITLE_SCREEN_HPP__
